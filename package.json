{
  "name": "farmguard",
  "version": "1.0.0",
  "description": "FarmGuard - Antimicrobial Usage Tracking & Risk Assessment",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node server.js",
    "install-server": "cd server && npm install",
    "install-client": "cd client && npm install",
    "install-all": "npm run install-server && npm run install-client",
    "seed": "cd server && node scripts/sync-db.js && node seeders/drugs-master.js && node seeders/demo-data.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
