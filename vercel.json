{
  "version": 2,
  "buildCommand": "npm run install-server && npm run install-client && cd client && npx vite build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
